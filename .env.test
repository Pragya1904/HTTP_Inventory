# docker compose --env-file .env.test up ...

# Use alternate host ports to avoid conflicts with local services.

RABBITMQ_PORT_HOST=5673
RABBITMQ_MGMT_PORT_HOST=15673
MONGO_PORT_HOST=27018

# App config (same keys as .env/.env.example)
DATABASE_HOST=mongo
DATABASE_PORT=27017
DATABASE_USER=
DATABASE_PASSWORD=

BROKER_HOST=rabbitmq
BROKER_PORT=5672
BROKER_USER=guest
BROKER_PASSWORD=guest

QUEUE_NAME=metadata_queue
QUEUE_MAX_LENGTH=1000

MAX_RETRIES=3
PREFETCH_COUNT=1

INITIAL_BACKOFF_SECONDS=1
MAX_BACKOFF_SECONDS=30
MAX_CONNECTION_ATTEMPTS=10

