[pytest]
addopts = -q
testpaths = tests
markers =
  integration: tests that require docker compose and real services
  failure_path: tests that expect 503 when rabbitmq/mongo are down (run with --no-deps after stopping deps)
asyncio_mode = auto

